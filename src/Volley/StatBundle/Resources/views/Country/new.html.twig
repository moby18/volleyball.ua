{% extends '::admin.html.twig' %}

{% block body -%}
    <h1>Country creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('stat_country') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
