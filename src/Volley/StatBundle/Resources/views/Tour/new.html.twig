{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tour creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tour') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
